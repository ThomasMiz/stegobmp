package grupo3.arguments;

/**
 * Specifies the possible actions a user can request to this program.
 */
public enum ArgumentRequest {
    /**
     * The user wants to embed a message into a carrier.
     */
    Embed,

    /**
     * The user wants to extract a message from a carrier.
     */
    Extract,
}
